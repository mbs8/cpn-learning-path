This command analyse the installed npm packages a and fixes vulnerabilities.

Do the following:

1. Run `npm audit` to find vulnerable installed packages
2. Run `npm audit fix` fix to apply updates
3. Run tests to verify the updates didn't break anything